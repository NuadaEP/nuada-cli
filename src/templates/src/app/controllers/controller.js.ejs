class <%= props.name %>Controller {
    async index(req, res) {
        return res.send('indexMethod');
    }
}

<% if (props.sucrase) { -%>
export default new <%= props.name %>Controller();
<% } else { -%>
module.exports = new <%= props.name %>Controller();
<% } -%>
