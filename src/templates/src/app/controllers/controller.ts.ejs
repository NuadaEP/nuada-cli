import { Request, Response } from 'express';

export default class <%= props.name %>Controller {
  public async index(request: Request, response: Response): Promise<Response<{ success: true }>> {
    return response.json({
      success: true
    });
  }
}
