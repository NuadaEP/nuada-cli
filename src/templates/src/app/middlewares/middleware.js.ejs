/**
 * Some middleware that will intercept requests before they reach the controllers
 */

//  class sampleMiddleware {
//      async index(req, res, next) {
//          if (true) return next()

//          return res.send(true)
//      }
//  }

// <% if (props.sucrase) { -%>
// export default new sampleMiddleware()
// <% } else { -%>
// module.exports = new sampleMiddleware()
// <% } -%>
